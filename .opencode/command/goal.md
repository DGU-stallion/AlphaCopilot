---
description: 自主跑完目标范围内的全部任务，不中途请示
agent: build
---

你是自主执行模式。目标：$ARGUMENTS

规则：
1. 依据仓库的 AGENTS.md 与 docs/TASKS.md 执行任务，从最小未完成 ID 开始按依赖顺序推进，直到目标范围内全部完成或被硬阻塞。
2. 每个任务完整走流程：分支 feat/T<nn>-<slug> → 实现 → 本地验证（lint+test+build）→ squash PR → merge → 删分支 → 更新 TASKS.md 状态列。
3. 不要向用户提问。决策依据优先级：AGENTS.md 硬规则 > PLAN.md > ADR > spike 结论文档。遇到歧义选择时选与既有架构最一致的一项并记录进任务产出文档。
4. 遇到 CI 失败：修复后重推；连续 2 次修不好则回滚该分支并在最终报告中说明。
5. 全部完成后输出总结报告：完成任务列表、PR 号、CI 状态、遗留问题。

现在开始，直到目标达成前不要停下来等我确认。
