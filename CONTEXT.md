# AlphaCopilot — 领域词汇表

> 本文件是纯词汇表，不包含实现细节。

## 核心概念

**AlphaCopilot**
个人量化投研系统。以 `AlphaTrading/` 下 Vibe-Research（投研看板 + 回测）与 vibe-astock
（A 股复盘）的现成能力缝合而成，本地单机运行。定位：确定性计算为主、AI 只解释。

**投研（Research）**
以人工阅读和判断为主的工作流：宏观看板、复盘看板（含涨停样本统计）、股票池、研报管理。
数据以确定性计算产出。

**量化（Quant）**
以程序化分析为主的工作流：相关性计算、回测、模拟组合。全部为确定性 Python 计算。

**全局 Agent（分析副驾）**
右下角浮标唤出的页面感知 chat panel。职责：解释当前页面数据、回答研究问题、提供
分析观点、总结研报。**不生成计算结果、不直接写数据库**（写操作走草案→用户确认）。
合规底线：不荐股、不预测涨跌、不给买卖时机。

**交互式图表（Interactive Chart）**
确定性计算返回结构化数据，前端用 ECharts 渲染的可交互图表（hover、缩放、时间范围）。

**页面感知上下文（Page Context）**
每个页面向 Agent 暴露「当前已展示的确定性数据快照」（含标的、区间、来源、样本数），
Agent 据此解释，不另起取数流程。

**宏观看板（Macro Dashboard）**
一屏看全宏观脸色，按五类分组：股市指数（A 股大盘 + 隔夜外围美股/港股 + 七姐妹）、
大宗商品（原油/黄金/白银，腾讯 gtimg）、债市（美债收益率曲线，美国财政部 CSV）、
汇率（主要货币对，腾讯 gtimg）、加密货币（BTC）。多数据源降级：哪个源可达用哪个，
都取不到则该类如实显示「暂不可用」，不伪造 0。

**复盘看板（Daily Review）**
盘面复盘一屏：市场情绪、今日/昨日打板情绪、连板梯队、成交额榜、板块资金轮动，
并入涨停样本统计（次日表现的确定性统计，非策略回测）。不含自选（自选见股票池）。

**回测（Backtest）**
对历史数据运行策略并评估表现。复用成熟回测引擎（逐 bar 撮合 + A 股市场规则 +
gate/run 守卫，移植自开源 HKUDS/Vibe-Trading，MIT，保留 NOTICE 归属）。
策略可插拔（`STRATEGIES` 注册表，现有双均线金叉，预留 vnpy 等外部引擎扩展位）。

**模拟组合（Simulated Portfolio）**
雪球式持续组合：标的 + 权重 + 调仓事件（生效日期 + 调权历史），从创建日记录净值，
默认基准沪深300。不涉及真实下单。

**相关性分析（Correlation）**
归一化走势叠加图 + 日收益率 Pearson 相关矩阵。基于收益率而非价格（避免伪相关），
多标的按交易日交集对齐。

## 导航分组

**市场复盘**：宏观看板 / 复盘看板（含涨停样本统计）

**研究管理**：股票池 / 我的研报

**量化研究**：回测 / 相关性分析 / 模拟组合

**个人与系统**：交易日志 / 接入 AI（设置）

> 全局 Agent 浮标独立于以上分组，覆盖在所有页面之上（UI 就绪，对话逻辑待 S5 接后端）。
