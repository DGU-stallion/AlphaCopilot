# AlphaCopilot — 领域词汇表

> 本文件是纯词汇表，不包含实现细节。

## 核心概念

**AlphaCopilot**
个人量化投研系统。整合 Vibe-Research（投研看板）与 Vibe-Trading（量化 agent 框架），本地单机运行。目标是"研究"与"量化"在同一界面内流转，由自然语言驱动。

**投研（Research）**
以人工阅读和判断为主的工作流：每日复盘、资讯雷达、个股数据查阅、板块跟踪、研报管理。数据来源以 `a-stock-data` 工具箱为主。

**量化（Quant）**
以程序化分析为主的工作流：自然语言驱动的 agent 分析、相关性计算、回测、因子研究。

**Agent 对话**
用户以自然语言提问，系统调度一个或多个专业 Agent 协作处理，结果以对话流形式从上到下呈现。图表作为消息嵌入对话流，可交互。

**分析师团队（Analyst Swarm）**
一次 Agent 对话中参与协作的多个专业 Agent（如数据分析师、量化分析师、可视化师）。每个 Agent 的工作步骤实时内联展示在对话流中，用户可见协作过程。

**交互式图表（Interactive Chart）**
Agent 返回结构化数据，前端使用 ECharts/Recharts 渲染的可交互图表（支持 hover、缩放、时间范围切换）。图表作为消息气泡嵌入对话流，不跳转新页面。

**上下文快捷入口（Contextual Agent Entry）**
在投研页面（如每日复盘、个股数据）内嵌的快捷 Agent 触发入口，预填当前页面上下文（如当前股票代码、当前日期）发起对话。

**回测（Backtest）**
对历史数据运行策略并评估表现。使用 Vibe-Trading 的回测引擎和数据管道。回测结果先在 Agent 对话流内渲染，后续可扩展独立报告页。

**回测数据管道（Backtest Data Pipeline）**
Vibe-Trading 提供的历史行情加载层，支持多市场、多数据源、fallback 链。与投研数据层（`a-stock-data`）并存，职责不重叠：前者服务回测，后者服务实时研究。

**Alpha 因子库（Alpha Zoo）**
来自 Vibe-Trading 的因子注册中心，包含学术因子和自定义因子，支持因子分析和策略开发。

## 导航分组

**投研组**：每日复盘 / 资讯雷达 / 板块中心 / 个股数据 / 自选股

**量化组**：Agent 对话 / 相关性分析 / 回测报告 / Alpha 因子库

**我的**：持仓 / 研报 / 研究记录 / 接入 AI
