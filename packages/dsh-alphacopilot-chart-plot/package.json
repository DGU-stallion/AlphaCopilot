{
  "name": "dsh-alphacopilot-chart-plot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AlphaCopilot interactive charts in dsh chat flow — chart/plot session event family + ECharts keyed renderer (ConversationNodeDefinition)",
  "main": "src/index.ts",
  "files": [
    "src/",
    "cordis.patch.yml"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir lib"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
