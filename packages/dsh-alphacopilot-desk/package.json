{
  "name": "dsh-alphacopilot-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Compliance prompt + domain skills + swarm preset config for dsh",
  "main": "lib/index.js",
  "files": ["lib/", "skills/", "presets/"],
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir lib"
  },
  "devDependencies": {
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2"
  }
}
